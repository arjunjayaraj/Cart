<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">


	<!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/admin**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/user**" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')" />
			<intercept-url pattern="**/registernew" access="permitAll" />

		<!-- access denied page -->
		<form-login 
				login-page="/login,http://localhost:8080/angularLogin/"
				default-target-url="http://localhost:8080/angularLogin/test.html" 
				authentication-failure-url="/login?error"
				username-parameter="email"
				password-parameter="password" />
		<logout logout-success-url="/login?logout" />
		<!-- enable csrf protection -->	
	<csrf/>
<custom-filter before="SECURITY_CONTEXT_FILTER" ref="myFilter"/>
</http>
<beans:bean id="myFilter" class="com.Cart.start.filter.CsrfHeaderFilter"/>
	<authentication-manager>
		<authentication-provider user-service-ref="loginService" />
	</authentication-manager>
</beans:beans>